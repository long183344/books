# 2026-08-26 工作日志

## Ch28 XLD 上卷收官(本日首次)

### 章节定位
- 第 28 章 XLD(eXtended Line Descriptions, 扩展线描述)
- 全章 6 子族 94 算子: Access 4 + Creation 12 + Features 45 + Sets 8 + Transformations 20 + Geometric Transformations 5
- **本卷(上) = Access + Creation + Features = 61 算子**
- 中卷下一刀 = Sets + Transformations = 28 算子
- 下卷 = Geometric Transformations = 5 算子

### 抽算子数对比截图 vs toc 精确核对
- 截图中 Features 罗列到 test_xld_point 截止,误判 49 → 经 toc 严格 = 45
- 关键陷阱:`select_xld_point` 和 `test_xld_point` 后缀是 `_point.html`(非 `_xld.html`),Strategy G 默认正则 `href="[^_]+_xld\.html"` 会漏掉 → **改用 `href="([a-zA-Z_0-9]+_xld(?:_point|_contours)?\.html)"`**

### HTML 结构修正(Strategy G v2)
- 本章 HTML **没有** `<div class="lead" id="lead">` 包裹,新近 Strategy G 的 `body_match` 锚定失效 → 直接全文找 `<h2 id="sec_synopsis">Signature</h2>`
- Signature 块包含多语言 div(`data-if="hdevelop"/"c"/"cpp"/"dotnet"/"python"`),首段 hdevelop 才是要的形式 — 在 `<code>` 块里匹配第一个含 `<b>op_name</b>` 的即可
- 平衡括号扫描从 op_name 后第一个 `(` 开始,depth 计数到 0 时截断

### 思维导图美学调整(三族三角辐射)
- 18×18 inch 大画布,中心 XLD 圆(7,7)小半径 1.05
- Access(11.8,12.3)右上 + Creation(12.0,4.5)右下 + Features(4.0,4.5)左下 — 三族错开避免压中心
- Features 45 ops 双列布局,Creation 12 ops 单列大字号 11,Access 4 ops 单列字号 12
- **极重要修正**:之前用 `fig.patches.append(Circle)` 渲染失败(`fig_level patches 不被 axes 绘制`),改成 `ax.add_patch(Circle/FancyBboxPatch/FancyArrowPatch)` + `ax.text()` 全 axes 层级才正确显示
- 主标题/脚标用 `fig.text()`(不被 axes 范围裁切)

### 产出
- `07-算法/HALCON/章节总结/28-XLD(上).md` (320 行)
- `07-算法/HALCON/章节总结/28-XLD(上).png` (三角辐射)
- README 双索引已更新(`07-算法/README.md` + 顶层 `README.md`)
- 7 段结构: 引言三段 → 全卷结构表 → 子族分述(Access/Creation/Features)+ 重点算子三段注 6 个 → 速查表 → 误区提示(7 条) → 3 段 HDevelop 经典 pipeline → 关联章节 → 一句话核心

### 6 个重点算子三段注
gen_contour_polygon_xld / gen_parallels_xld / fit_circle_contour_xld / fit_line_contour_xld / area_center_xld / smallest_rectangle2_xld

### 3 段 HDevelop pipeline
1. **PCB 走线宽**:`edges_image`(canny) → `gen_parallels_xld`(gradient) → `fit_line_contour_xld` + `select_contours_xld` 过滤
2. **圆度判定金标准**:`gen_contour_region_xld` 上桥 → `fit_circle_contour_xld`(algebraic) → `circularity_xld`
3. **任意方向矩形配准**:`select_contours_xld` 选轮廓 → `smallest_rectangle2_xld`(出 Phi) → `vector_angle_to_rigid` + `affine_trans_contour_xld`(预告下卷)

### Git 提交
- commit `1fe3f28` docs(halcon-ch28-upper): 第 28 章 XLD 上卷 (Access+Creation+Features, 61 算子)
- 4 files changed: `28-XLD(上).md` (新) + `28-XLD(上).png` (新) + `07-算法/README.md` + `README.md`
- 已 push,远端 SHA `1fe3f28fab1de197d07f43066d3e890ca2ae31cf` 与本地一致

### 待办跟进
- Ch28 中卷:Sets(8) + Transformations(20) = 28 算子,预计 1-2 次截图即可
- Ch28 下卷:Geometric Transformations 5 算子(单卷很小,可与中卷合卷)
- 续完成 Ch17 Matching 下卷, Ch20 OCR 下卷,以及 Ch29+待确认

## Ch28 XLD 下卷收官(本日第二次)

### 章节定位修订
- 原来误判中下卷分=三卷(Sets+Transformations / Transformations / Geometric Transformations),实际用户合并到**下卷** = Sets + Transformations + Geometric Transformations
- 实际下卷 = **34 算子** = Sets 8 + Transformations 20 + Geometric Transformations 6
- 几何变换**6**(不是 5,因为 toc 解析漏了 `polar_trans_contour_xld_inv`,Strategy G v2 正则 `([a-zA-Z_0-9]+_xld(?:_point|_contours)?)` 没覆盖 `_xld_inv` 后缀 → **再次修正**:正则扩为 `([a-zA-Z_0-9]+_xld(?:_inv|_point|_contours)?)`

### 全章算子数
- 修订后:Access 4 + Creation 12 + Features 45 + Sets 8 + Transformations 20 + Geometric Transformations 6 = **95 ops**(原来误判 94)
- 上 61 + 下 34 = **95 ops** 收官

### 思维导图布局调整
- 同一放射布局但配比不同(中间大族 20 ops 双列→3 列已易超界)
- 改为:中心 XLD 圆半径缩到 0.85,以腾出空间
- Sets(8 ops)右上 11.6,12.3 / Transformations(20 ops)左下 3.6,4.5 改 3 列字号 10.5 / Geometric Transformations(6 ops)右下 13.2,6.5
- 双列→3 列切换,Transformations 卡 box_w 从 9.5 降到 8.5

### 4 重点算子三段注
- `gen_parallel_contour_xld`(沿已知轮廓的方向偏移,Mode='fixed',区别于上卷的 gen_parallels_xld 基于灰度)
- `affine_trans_contour_xld`(HomMat2D 输入对齐)
- `smooth_contours_xld`(NumRegress 5~15,过大圆化尖角)
- `union_collinear_contours_ext_xld`(MaxCoLLinDeviation 1.5~3 像素,标准 vs ext 区别)

### 3 段 HDevelop pipeline(下卷主题)
1. **配准后差分**:`gen_contour_region → close_contours → difference/intersection/symm_difference_closed_contours` — 检测 defects
2. **PCB 长直走线测量**:`edges_image → edges_sub_pix → split_contours → smooth_contours → union_collinear_contours_ext → fit_line_contour` — 总长直线提取
3. **任意方向矩形归一**:`union_adjacent_contours → select_contours → smallest_rectangle2 → vector_angle_to_rigid → affine_trans_contour → polar_trans_*` — 摆正 + 圆环展开

### Bash + Python 字符串双引号陷阱(本日第二次踩坑)
- `_tmp_ch28_lower_build_md.py` 脚本里,中间某段 HDevelop 块插入 `append("")` 裸语句(本意 `md.append("")`),导致 `NameError: name 'append' is not defined`
- 简单修复:Python str.replace 替换 `\nappend("")` → `\nmd.append("")`,避免重写整个脚本
- **根因预防**:以后编写 Python 长脚本,统一把所有工具调用包一层 helpers(`md1("...")`),避免漏前缀

## HALCON 技能封装(本日第三次)

### 触发
用户: "把相关本地 halcon 官方文档的阅读方式，算子分类快速查询，读取方法，关键点读取和halcon 语言的总结经验，halcon 的使用算法的整理总结，写成一个skill"

### 产出
- 技能名 `halcon-master`(用户级 ~/.workbuddy/skills/halcon-master/)
- SKILL.md(8K,六维能力 + workflow + 关键坑)
- references/7 文件(40K):
  - doc_structure.md — 文档结构与多语言清洗
  - operator_catalog.md — Ch1–28 算子地图
  - extraction_strategy.md — Strategy G 规格
  - key_points.md — 9 类陷阱(in-place / 半开 / _ext / tuple 语义 / 坐标系 / 拟合算法 / 创建类对 / 集合前置 / 伪链接)
  - halcon_language.md — HDevelop + 4 大数据结构
  - algorithm_patterns.md — 9 条可复用视觉 pipeline
  - chapter_template.md — 7 段 markdown 骨架
- scripts/3 文件(20K):
  - parse_toc.py — 兼容 _xld_inv/_point/_contours 后缀
  - extract_operators.py — Strategy G 抽取器
  - build_mindmap.py — matplotlib 辐射思维导图
- 已打包 halcon-master.zip(26K) 通过 skill-creator 校验

### 关键设计
- **YAML frontmatter**:`description: |-` 而非 `>-`(后者含 `>`,触发校验器 "angle brackets" 误报)
- **字体加载**:`fm.fontManager.addfont(SIMHEI)` 而非 `addfile`(后者不存在),同时设 `rcParams['font.sans-serif']=['SimHei',...]` 兜底
- **描述抽取三级**:①段首 `<p>` + DESC_HINTS 关键词(主);②首合格 `<p>` 兜底;③整段清洗 + 去连续重复 + 截取首个描述动词(终极兜底,应对签名回放与描述同段)
- **布局**:cols*1.95 + 0.6 列宽, ROW_H 0.36 行高, fs 按 n 分档(10/9/8)

### 验证
- parse_toc:toc_xld_features.html → 45 ops ✓
- extract_operators:edges_sub_pix / fit_circle_contour_xld / smallest_rectangle2_xld / gen_contour_polygon_xld 全部正确抽取描述 ✓
- build_mindmap:3 子族 46 ops 中文渲染,无重叠 ✓

