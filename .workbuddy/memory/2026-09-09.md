# 2026-09-09 工作日志

## 08-经验 文档仓库内搬家 + 删旧 + 推送
- 用户要求：把 `08-经验` 里 33 个 .docx 全部复制一份、删除原有、上传仓库
- 关键核查：这 33 个文件此前**从未被 git 跟踪**（git ls-files 为 0），GitHub 远端本来就没有 → 触发澄清，用户选择「仓库内搬家+删旧+推送」
- 操作：复制到新目录 `08-经验-整理/`（33 docx + 自动生成 README.md 索引）
- **数据安全校验（重要经验）**：初看 `stat` 大小每个差 4096、且 2 个文件目标为 0 字节 → 误以为复制损坏。实则为 **NTFS 备用数据流/稀疏元数据**（不属于 docx 正文，GitHub 也不会存）；用 `md5sum` 全量比对，33 个正文哈希 100% 一致，确认无损后才删源。两个 0 字节文件 md5=空文件哈希，源里本就是空文档正文
- 删旧目录 `08-经验`，根 README 分类导航增补 08 条目
- git commit `3023f83`（35 文件），远端 SHA 校验一致：`3023f839bfb71e6b37a053576409bfce536b3706`
- 教训：Windows 上 `cp` 不复制 ADS，导致 stat 大小与 cmp/md5 内容不一致；**删除大批量文件前务必用 md5/cmp 验证副本完整性，不可仅凭 stat 大小判断**
