## 2026-08-25（Ch26 Transformations 上卷）

### Ch26 上卷：2D Transformations(32) + 3D Transformations(19) = 51 ops
- 截图算子数：2D = 32 (affine_trans_pixel, affine_trans_point_2d, deserialize_hom_mat2d, 15 个 hom_mat2d_basic, hom_vector_to_proj_hom_mat2d, point_line_to_hom_mat2d, projective_trans_pixel/point_2d, serialize_hom_mat2d, 8 个 vector_to_*); 3D = 19 (affine_trans_point_3d, deserialize_hom_mat3d, 11 个 hom_mat3d_*, hom_mat3d_to_pose, pose_to_hom_mat3d, projective_trans_hom_point_3d/point_3d, serialize_hom_mat3d, vector_to_hom_mat3d)
- TOC 实际列了 2D=60（其中 28 个是跨章节应用算子如 affine_trans_image/region、polar_trans_*、find_*_model 等不属于 Ch26 原生算子），3D=19 = 截图
- Ch26 全章 6 子族 119 ops：2D(60)/3D(19)/Dual Quaternions(10)/Misc(2)/Poses(19)/Quaternions(9)
- 严格按用户截图 = **本卷 51 ops**，下卷 = Dual Quaternions(10) + Misc(2) + Poses(19) + Quaternions(9) = 40 ops

### 关键差异：toc_transformations_*.html 是「介绍页」而非「算子清单」
- 它的 H2 唯一：`h2>2D Transformations</h2>`
- 内容是 HALCON 解释 2D 变换的概念、用例、相关算子链接
- 真实"章节归属"由 toc_transformations.html 主索引定义

### 重点算子（29 详细备注算子）
- 2D 重点：`hom_mat2d_identity/compose/invert/determinant/transpose/rotate/rotate_local/translate_local/scale_local/slant/reflect/reflect_local/to_affine_par` (代数 + 局部变换)、`vector_to_rigid/similarity/aniso/hom_mat2d/proj_hom_mat2d/proj_hom_mat2d_distortion/angle_to_rigid/point_line/field` (反算)、`affine_trans_pixel/point_2d` (应用)、`serialize/deserialize_hom_mat2d`
- 3D 重点：`hom_mat3d_identity/compose/determinant/invert/transpose/rotate/rotate_local/scale/scale_local/translate/translate_local`、`hom_mat3d_to_pose`/`pose_to_hom_mat3d`、`affine_trans_point_3d`、`projective_trans_hom_point_3d`、`serialize/deserialize_hom_mat3d`、`vector_to_hom_mat3d`

### Git
- commit `b6e4334` — 远端 SHA `b6e433498d23fee3a33501440006e286e0f34c64` 一致

## 2026-08-25（Ch26 Transformations 下卷）
- 完成 Ch26 下卷：4 子族 40 ops — Poses(19)+Quaternions(9)+Dual Quaternions(10)+Misc(2)
- 抽取策略修正：签名算子名是 `<b>op_name</b>` 而非 `<a id="op...">`，HTML 多语言 span 用 `<span data-if>` 包裹（hdevelop=inline，其余 display:none），清洗需先删 display:none 的 span/div 再剥标签
- 产出：`26-Transformations(下).md` + `26-Transformations(下).png`（四角辐射思维导图）
- commit `907c009` — 远端 SHA `907c009c6006010517c5d510e78078985f9f3ee7` 一致
- Ch26 全章 6 子族 91 ops 收官（上 51 + 下 40）

## 2026-08-25（Ch27 Tuple 上卷）
- 完成 Ch27 上卷：3 子族 63 ops — Arithmetic(45)+Bit Operations(6)+Comparison(12)
- Ch27 全章 14 子族共 154 ops，下卷 = 91 ops（Containers/Conversion/Creation/ElementOrder/Features/LogicalOperations/Manipulation/Selection/Sets/StringOperations/Type）
- 抽取脚本稳定（沿用 Strategy G + <b>op_name</b> 锚点 + 先删 display:none span/div）
- Mindmap 3 子族 = 三角辐射（顶部 Arithmetic 5×9，左下 Bit 3×2，右下 Comparison 3×4），底部 legend/info 用 fig.add_axes() 而非 ax.text 避免被 axes 范围裁切
- 关键修正：① ax.text 放在 ylim 边缘会被 axes 裁切，底部说明框必须用 fig-level axes (fig.add_axes) ② FancyBboxPatch boxstyle pad 越小标题与第一行 chip 间距越稳 ③ findfont bold 警告可忽略（最终用非 bold weight）
- 重点算子（25 详细三段注）：
  - Arithmetic：`add/mult/sub/div/cumul/pow/sqrt/log/atan2/deg/rad/max2/min2/fmod/hypot/sgn/ceil/floor/erf/lgamma/tgamma/neg`
  - Bit Operations：`band_pitfall` 通用坑（仅整数）
  - Comparison：`equal_elem/greater/greater_elem/less_elem/not_equal_elem`
- 产出：`27-Tuple(上).md`（27KB）+ `27-Tuple(上).png`（三角辐射美学思维导图）
- commit `e2b71f9` — 远端 SHA `e2b71f95385c568471f7bdbe851c3de74d080eec` 一致

## 2026-08-25（Ch27 Tuple 中卷）
- 完成 Ch27 中卷：7 子族 47 ops — Conversion(12) + Creation(5) + Containers(10) + ElementOrder(2) + Features(11) + Logical Operations(4) + Manipulation(3)
- ⚠️ 截图把 `tuple_sort_index` 列在 ElementOrder 下，但官方 toc_elementorder.html **只有 2 ops** (tuple_inverse + tuple_sort)，tuple_sort_index 实际归下卷 Selection。中卷严格按 toc = 47 ops。
- 重点算子（约 18 个三段注）：
  - Conversion：`tuple_string`（C printf 格式）/`tuple_number`（失败抛错不像 Python）/`tuple_round`（二进制舍入）/`tuple_chr/chrt/ord/ords`
  - Creation：`tuple_gen_const/sequence`（含 End 从 Start 加 Step 直到越过）/`tuple_rand`（设负 Seed 才能复现）
  - Containers：`create_dict` + `set/get_dict_tuple`（⚠️ Key 含小数点会被嵌套访问：`a.b` = dict['a']['b']）+ `read/write_dict`（.hdict 私有协议 ≠ JSON）
  - ElementOrder：`tuple_sort`（⚠️ 字典序 'Z'<'a'，混合类型抛错）+ `tuple_inverse`（immutable）
  - Features：`tuple_min/max/sum/mean/median/deviation`（⚠️ 默认总体 σ 除 N，统计教材用样本 σ 除 N-1）+ `tuple_histo_range`（⚠️ 桶不包含 Max，左闭右开）
  - Logical：`tuple_and/or/xor/not`（⚠️ 仅整数）
  - Manipulation：`tuple_insert`（Index 超界**夹紧**不抛错）+ `tuple_remove`（半开区间 [I1,I2)）+ `tuple_replace`（长度可变）
- Mindmap 7 子族 = 七瓣辐射（Conversion 橙 / Creation 青 / Containers 紫 / ElementOrder 红 / Features 蓝 / Logical 绿 / Manipulation 深红），中心圆「Tuple 中卷 47 ops」
- 关键修正：图底部 fig-level 渲染（fig.text + fig.patches.append）替代 ax.text，避免被 axes 范围裁切
- 产出：`27-Tuple(中).md` + `27-Tuple(中).png`（七瓣辐射美学思维导图）
- commit `36fdb16` — 远端 SHA `36fdb16d3dd44d32ecfac1224d069682cc38d0e5` 一致
- Ch27 已收官：上卷 63 + 中卷 47 = 110 ops（**下卷 44 ops 待办**：Selection 11 + Sets 4 + StringOperations 15 + Type 14）
