
## 知识思维导图目录建立(本日)

### 触发
用户: "在我的git 仓库目录下，创建知识思维导图目录，画一张关于所有PLC（三菱，信捷，3u，5u），电路板，开发板，fpga, 树莓派，单片机，电脑主机，这些的关系介绍，相关介绍，要详细），并推送" + 手动绑定 skill「可视化｜思维导图｜简诗 AI」

### 产出
- 目录 `知识思维导图/`(git 仓库根下)
- 文件:
  - `硬件知识体系.puml`(2.6K, PlantUML mindmap 源)
  - `硬件知识体系.svg`(40K, 矢量, viewBox 4916x335)
  - `硬件知识体系.png`(110K, 栅格 4096x334, kroki 渲染)
  - `硬件知识体系.md`(5K, 关系速览表 + 内嵌 plantuml 代码块)
  - `README.md`(目录约定 + 已收录表 + 工具链 + 新增主题模板)
- 顶层 README.md 加一行指向新目录

### 关键工程经验(本次沉淀)
- **无 Java/PlantUML 本地**:使用公共 kroki.io (POST https://kroki.io/plantuml/{svg|png}, Content-Type: text/plain, body=源码) 渲染
- **Node fetch + Windows 路径**:`C:/...` 才能被 node 解析;`/c/Users/...` 会变成 `e:/c/Users/...` 找不到;Curl `--data-binary @file` 在此 shell 也不稳,统一用 Node + stdin body
- **kroki 输出尺寸硬上限** PNG 4096px(viewBox 4916→PNG 4096,自动等比缩 0.83)
- **`scale 0.x` 预处理器对 kroki 无效**(渲染结果与不加 scale 完全一致,SVG viewBox 不变)
- **mindmap 宽度由「一级分支数 × 每支最深叶节点文本长度」决定**:
  - 8 一级 + max depth 4 + 短文本(≤10字)→ viewBox 4916,可接受
  - 6 一级把硬件载体合并→ viewBox 6491 **更宽** 因为合并支变胖,反而变差
  - 结论:分支越多越窄,前提是每支本身瘦;不要为了减少分支数而把内容合并
- **`left side` 平衡**:4 左 + 4 右 比全右(或全左)窄一半左右,关键
- **font 10 + padding 3** 比默认 11 紧凑 ~30%

### 推送
- commit 推送待执行

## 知识思维导图 重画(本日第二次, matplotlib 方案)

### 触发
用户: "重新写，你要讲清除plc，开发板，电路板，这些的区别和联系，详细解析plc，思维导图重新画" + 手动绑定新 skill「思维导图生成skill」

### skill 真相
- **「思维导图生成skill」(mindmap-generator-plus) 是 Matplotlib 版**,不是 PlantUML！中心→终端→模块→功能项 4 层；辐射布局(3-4角/5边/6边/7边+子组box/8边+自适应卡片)。
- 中文靠 `fm.fontManager.addfont('C:/Windows/Fonts/simhei.ttf')` 加载 SimHei。
- 数据用 `FAMILIES = [(name,color,sub_desc,[items...]), ...]`。
- 自适应边界: 收集所有 box 坐标算 min/max 再 set_xlim/ylim,避免裁切。

### 设计(5 族辐射)
- 中心: PLC·开发板·电路板 区别与联系
- 5 族: PLC详细解析(橙★) / 开发板(绿) / 电路板(蓝) / 三者区别与联系(红) / 关联硬件(紫)
- 强调区别与联系: 在红框列出「电路板无智能/开发板有智能开发者用/PLC智能+防护」「联系①②③」「控制力递进」

### 关键坑
- **SimHei 缺 U+2282 ⊂(SUBSET OF) 字形** → 渲染成豆腐块。改用中文「开发板 属于 电路板」(属/于是常用汉字)。其余 ·/★ 正常。
- 目录被莫名删除: 提交后 `知识思维导图/` 整个从磁盘消失(git index 还在)。用 `git checkout -- 知识思维导图/` 从 fe9b3fe 恢复旧文件,再重跑 .py 重新生成 .png/.svg,重写 .md/README.md,git rm .puml。原因不明(疑似中文目录名在 Git Bash 偶发路径问题),下次若再遇同样"目录没了"先 `git checkout -- <dir>` 恢复,不要重头来。

### 文件变更
- 删除 `硬件知识体系.puml`(PlantUML 源, 已过时)
- 新增 `硬件知识体系.py`(matplotlib 源, 可复跑)
- 重写 `硬件知识体系.md`(区别对比表 + PLC 详解表 + 关联硬件) / `README.md`(工具链改 matplotlib) / 顶层 README 一行
- 重生成 .png(2689x2802) / .svg
- commit `babd478` 推送, SHA `babd4783889fa3427d987de67a2a4f5b90b7b531`
