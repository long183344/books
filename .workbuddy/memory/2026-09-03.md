# 2026-09-03 工作日志

## BlueScreenView 蓝屏崩溃分析文档
- 用户要求：编写 BlueScreenView 加载 `C:\Windows\Minidump` 崩溃日志的详细使用文档 + 蓝屏原因检测步骤，并推送仓库，自决目录
- 目录决策：放 `02-操作系统与底层/Windows崩溃诊断/01-BlueScreenView蓝屏崩溃日志分析.md`
  - 理由：BSOD 是 Windows 内核崩溃，属 OS 底层诊断；02 分类当时 `🌱 待填充`，作为首篇最合适
  - 新建子目录 `Windows崩溃诊断/` 以容纳后续崩溃相关主题
- 文档结构（遵循 `_模板/笔记模板.md` 风格）：
  1. 一句话结论 + 数据基准/难度/前置
  2. 核心原理（KeBugCheckEx 流程 ASCII 图 + 5 种 dump 类型对照 + BlueScreenView 地址→驱动映射机制）
  3. 工具横向对比（BlueScreenView/WhoCrashed/WinDbg/可靠性监视器/事件查看器）
  4. 详细使用步骤（前置开小内存转储 → 获取 → 加载 → 界面 → 关键列 → 命令行 /scomma 等 → 高级选项）
  5. 蓝屏原因检测 8 步法（含跨多次崩溃找共性驱动）
  6. STOP 代码速查表（18 条）
  7. 3 个实战案例（显卡/杀软/硬件内存）
  8. 6 条常见误区
- 同步更新 `02-操作系统与底层/README.md`（新增"已收录主题"段）与根 `README.md`（02 状态改为"起步"）
- git commit `88f7ddf`，远端 SHA 校验一致：`88f7ddf5dba7585a30c9f8242f9f526999e57671`
