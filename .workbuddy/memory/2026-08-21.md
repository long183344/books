# 2026-08-21 工作日志

## 第 4 章 3D Object Model 算子体系总结（提交 4b79a54）

- 用户要求：发截图（第 4 章 3D Object Model，4 大族 × 52 算子），并要求用 mindmap-generator-plus skill 生成思维导图，"小目录要有原来的英文，算子列表"
- 算子拆解（按用户截图逐字对齐）：
  * 族 A · Creation（18）：clear/copy_object_model_3d, deserialize_, gen_box/cylinder/empty/sphere(_center)/plane_, gen_object_model_3d_from_points, read_, remove_*_attrib(_mod), serialize_, set_*_attrib(_mod), union_, write_object_model_3d
  * 族 B · Features（10）：area_, distance_, get_object_model_3d_params, max_diameter_, moments_, select_, smallest_bounding_box_, smallest_sphere_, volume_, volume_*_relative_to_plane
  * 族 C · Segmentation（4）：fit_primitives_, reduce_*_by_view, segment_, select_points_
  * 族 D · Transformations（20）：affine_trans_, connection_, convex_hull_, edges_, fuse_, intersect_plane_, object_model_3d_to_xyz, prepare_, project_, projective_trans_, register_*_pair/global, render_, rigid_trans_, sample_, simplify_, smooth_, surface_normals_, triangulate_, xyz_to_object_model_3d
  * 合计 52 算子
- 产物：
  - 07-算法/HALCON/章节总结/04-3D对象模型.md（432 行，26.6 KB）
  - 07-算法/HALCON/章节总结/04-3D对象模型.png（462 KB，mindmap-generator-plus skill 生成：中心 3D Object Model，4 族分布在四角，每个算子英文原名作为子节点）
  - 索引更新：07-算法/README.md 加第 4 章行；根 README 第 4 章链接 +07 计数 4→5
- 关键认知（用户之前不知道的，本章必须强调）：
  - ObjectModel3D 是**多态句柄**（不是结构体），可装点云/triangulation/normals/mapping/attrib 五种数据形态
  - `xyz_to_object_model_3d` 的 `XYZMapping` 有 6 种：`'cartesian'`/`'polar'`/`'cylindrical'`/`'spherical'`/`'object_polar'`/`'plane_and_height'`/`'onto_plane'`（最后一种要配 Mapping 矩阵——结构光相机/ToF 入口）
  - `volume_object_model_3d` 不带参考平面会算无意义的"全空间体积"，必须用 `volume_object_model_3d_relative_to_plane`
  - `union_object_model_3d`（集合并）vs `fuse_object_model_3d`（去重+重采样）很容易混
- 推送：4b79a54，SSH 推送成功，本地与远端 SHA 完全一致 ✅

## 第三章可访问性答疑（commit 未变更，仅核查）

- 用户反映"在仓库里没看到第三章"
- 核查：本地 03-3D匹配.md 存在（44395 字节）、本地 HEAD = 远端 main = d419d93（SHA 一致）
- WebFetch 验证 raw.githubusercontent.com 可正常访问该文件，确认文件确实在仓库
- 给用户精确 blob 链接（中文路径 URL 编码）和 raw 链接
- 结论：本地和远端都已确认有第三章；用户可能是浏览器缓存/账号问题，已提�� Ctrl+F5 强刷

## 第 5 章 3D Reconstruction 算子体系总结（提交 df35570）

- 用户要求：发截图（第 5 章 3D Reconstruction，5 大族），继续用 mindmap-generator-plus skill 出图
- 算子拆解（按用户截图逐字对齐）：
  * 族 A · Binocular Stereo（24）：binocular_disparity(_mg/_ms), binocular_distance(_mg/_ms), disparity_image_to_xyz, disparity_to_distance/point_3d, distance_to_disparity, essential_to_fundamental_matrix, gen_binocular_proj_rectification, gen_binocular_rectification_map, intersect_lines_of_sight, match_*_ransac 4 个, reconstruct3d_from_fundamental_matrix, rel_pose_to_fundamental_matrix, vector_to_* 4 个
  * 族 B · Depth From Focus（2）：depth_from_focus, select_grayvalues_from_channels
  * 族 C · Multi-View Stereo（10）：clear/create_stereo_model, get/set_stereo_model_image_pairs/object/object_model_3d/param, reconstruct_points_stereo, reconstruct_surface_stereo
  * 族 D · Photometric Stereo（12）：estimate_al_am, estimate_sl_al_lr/zc, estimate_tilt_lr/zc, photometric_stereo, reconstruct_height_field_from_gradient, sfs_mod_lr, sfs_orig_lr, sfs_pentland, shade_height_field, uncalibrated_photometric_stereo
  * 族 E · Sheet Of Light（17）：apply_sheet_of_light_calibration, calibrate_, clear_, create_sheet_of_light_calib_object/model, deserialize_, get_sheet_of_light_param/result/result_object_model_3d, measure_profile_, query_, read_, reset_, serialize_, set_profile_, set_param, write_
  * 合计 65 算子
- 产物：
  - 07-算法/HALCON/章节总结/05-3D重建.md（414 行，23 KB）
  - 07-算法/HALCON/章节总结/05-3D重建.png（716 KB，**五角形辐射布局** mindmap，中心 3D Reconstruction，5 族分布在 5 个方向，65 算子英文全列）
  - 索引更新：07-算法/README.md 加第 5 章行；根 README 第 5 章链接 +07 计数 5→6
- 关键认知（HALCON 3D 能力分层的"传感器层"）：
  - 本章 = 把 2D 图像反推为 3D 几何，区别于 Ch.3（用 3D 模型匹配找位姿）、Ch.4（整理 3D 数据）
  - 5 种物理路径互补：双视差/多焦距/多视角/多光源/线激光
  - 视差计算三件套：binocular_disparity（默认单尺度）vs *_mg（多网格）vs *_ms（多尺度+SGM，量产首选）
  - 几何矩阵三剑客：EssentialMatrix（标定后归一化系）vs FundamentalMatrix（含内参未标定）vs RelPose（7 元 6DOF）
  - 关键陷阱：`disparity_to_distance` 必须传**校正后**的 RelPoseRect，不是标定时的 RelPose
  - SFS 三种算法：sfs_orig_lr（经典 Woodham 平面）vs sfs_mod_lr（鲁棒量产推荐）vs sfs_pentland（频域）
- 推送：df35570，SSH 推送成功，本地与远端 SHA 完全一致 ✅
- 跨章数据流：Ch.5 重建 3D → Ch.4 整理 → Ch.3 匹配找位姿 → Ch.9 可视化

---

## 第六章 Calibration（`6c8e27f`，未推送前在 df35570 之后）

- 按截图整理全章 64 算子 → 10 子族（架构完全按本地 Operator Reference 核对）
  * A · Binocular (1): binocular_calibration
  * B · Calibration Object (8): caltab_points / create_caltab / disp_caltab / find_caltab / find_caltab_object / find_marks_and_pose / gen_caltab / sim_caltab
  * C · Camera Parameters (6): cam_mat_to_cam_par / cam_par_to_cam_mat / deserialize_cam_par / read_cam_par / serialize_cam_par / write_cam_par
  * D · Hand-Eye (4): calibrate_hand_eye / get_calib_data_observ_pose / hand_eye_calibration / set_calib_data_observ_pose
  * E · Inverse Projection (1): get_line_of_sight
  * F · Monocular (1): camera_calibration
  * G · Multi-View (27): calibrate_cameras / create_calib_data / get_calib_data_*×3 / set_calib_data_*×4 / read/write/serialize/deserialize_calib_data / camera_setup_model×6 / query_calib_data / remove_calib_data×2
  * H · Projection (4): cam_par_pose_to_hom_mat3d / project_3d_point / project_hom_point_hom_mat3d / project_point_hom_mat3d
  * I · Rectification (9): change_radial_distortion_*×4 / contour_to_world_plane_xld / gen_image_to_world_plane_map / gen_radial_distortion_map / image_points_to_world_plane / image_to_world_plane
  * J · Self-Calibration (3): radial_distortion_self_calibration / radiometric_self_calibration / stationary_camera_self_calibration

- 核心抽象：『世界 → 相机 → 像素』几何管焊死（前置几何层，Ch.1/2/3/5/7 都要靠本章）
- 三套 API 路线对比（本章最重要的认知）：
  * 路线 A 老 Flat-Tuple API（camera_calibration / binocular_calibration / hand_eye_calibration）— 教学/单/双目
  * 路线 B 新 CalibData 模型 API（create_calib_data + set_calib_data_* + calibrate_cameras）— 多相机生产（推荐）
  * 路线 C Self-Calibration（无标定板现场 3 法：径向畸变 / 响应曲线 / 相机内参）
- 用户特别要求：『你要仔细查看这些算子的对应文档，以文档内容为准』 → 抽 62/64 算子的真实签名 + GenParam，发现 2 个差异已写入文档：
  * 截图里 `get_calib_data_observ_poses` 与 `set_calib_data_observ_poses`（复数）在 HALCON 20.11.1.0 文档中是单数 `*_observ_pose`
  * `find_caltab_object` 已被 `find_caltab` 取代（老 API，预留兼容但用户应直接用新 API）
- 4 实战 HDevelop 完整案例：单相机 6 张图测尺寸 / 双目 12 张图给 Ch.5 / Eye-in-Hand 手机 + 机器人末端 / 鸟瞰传送带测量
- 12 误区按危险度排序（含混用 A/B API、StartCam 给 0、FindMarks 不收敛、Error > 0.5 直接上线等）
- 思维导图：10 列垂直栈（一族一列），10 族 header + 全部算子英文名，最大列 G 27 项不裁剪；标题 + 底部 WorkFlow 横向图
- 关键 GenParam 选中：camera_calibration.EstimateParams / create_calib_data.CalibSetup / hand_eye_calibration.Method / gen_image_to_world_plane_map.Scale / change_radial_distortion_cam_par.Mode / radiometric_self_calibration.FunctionType 等
- 推送：6c8e27f（从 df35570 → 6c8e27f，+825/-1，4 文件），SSH 推送成功，本地与远端 SHA 完全一致 ✅
- 与其他章关系：Ch.6 RelPose → Ch.5 disparity_to_distance；Ch.6 CameraParam → Ch.2 Metrology Pose；Ch.6 ImageToWorldPlane → Ch.1 1D 测量单位换算

## 第 7 章 Classification 算子体系总结（提交 9f1a84e）

- 用户要求：发截图（Ch.7 Classification，6 族 101 算子），并明确说『有点多，有些不常用的简单说明就行』 → 文档精简 + 不堆 101 算子名字
- 算子拆解（按截图严格对位 20+18+5+11+24+23=101）：
  * A · GMM（20）·create_class_gmm, train_, classify_, add_class_train_data_gmm, get_prep_info_, get_params_, select_feature_set_gmm, evaluate_class_gmm + 共用 7 个 IO/destroy 算子
  * B · KNN（18）·create_class_knn, train_, classify_, set_params_class_knn（族特有）, select_feature_set_knn + 共用 7 个
  * C · MLP（24）·create_class_mlp, train_, classify_, set/get_regularization_params_class_mlp, set/get_rejection_params_class_mlp（族特有 4 个）+ 共用 7 个
  * D · SVM（23）·create_class_svm, train_, classify_, reduce_class_svm + get_support_vector*_class_svm（族特有 3 个）+ 7 个共用
  * E · LUT（5）·clear_class_lut, create_class_lut_{gmm,knn,mlp,svm}
  * F · Misc（11）·create_class_train_data, add/get/read/write/serialize/deserialize/select_sub_feature/set_feature_lengths/clear/get_sample_num
- 核心抽象（用户要求『简单说明就行』）：101 个算子 ≈ 4 套分类器共享 5 件套 = create_class_X → 样本（add_sample 直接加或 ClassTrainData 共享袋）→ train_class_X → classify_class_X → write/read/serialize
- 抽 21 个关键算子的真实签名（按本地文档）：
  * create_class_gmm(:NumDim, NumClasses, NumCenters, CovarType, Preprocessing, NumComponents, RandSeed :GMMHandle)
  * create_class_knn(:NumDim :KNNHandle)（最简！）
  * create_class_mlp(:NumInput, NumHidden, NumOutput, OutputFunction, Preprocessing, NumComponents, RandSeed :MLPHandle)
  * create_class_svm(:NumFeatures, KernelType, KernelParam, Nu, NumClasses, Mode, Preprocessing, NumComponents :SVMHandle)
  * reduce_class_svm(:SVMHandle, Method, MinRemainingSV, MaxError :SVMHandleReduced)
  * create_class_lut_*(:Handle, GenParamName, GenParamValue :ClassLUTHandle)
- 关键 GenParam 取值全部按本地文档提取：
  * GMM.CovarType=spherical/diag/full；Preprocessing=none/center/normalize/principal_components/canonical_variates
  * MLP.OutputFunction=softmax/logistic/linear（SVM/Multi-class 必须 softmax）
  * SVM.KernelType=rbf/linear/polynomial/sigmoid；KernelParam 是 γ/degree/coef0
  * reduce_class_svm.Method=bottom_up/top_down；SVM.Mode=one-versus-one/one-versus-all/novelty-detection
- 6 族选型决策树 + 5 个真实工程案例表（AOI/OLED 亮点/KNN 印章/LUT 抽检/多模态样本袋）
- 10 实战误区按危险度排序（NumDim 必须匹配特征长度；ClassID 不能负数且默认 0=背景类；SVM OvR 必须 ≥2 类；GMM Density 不直接当概率用；KNN 推理慢；LUT 不可增量更新等）
- 思维导图（用 mindmap-generator skill 六边形辐射）：中心 Classification + 6 族六向放射 + 每族 5 件套 + 族特性 + 关键参数；底部统一模板长条 + IO 公共算子
- 推送：9f1a84e（从 6c8e27f → 9f1a84e，+463/-1，4 文件：460 行 md + 443KB PNG），SSH 推送成功，本地与远端 SHA 完全一致 ✅
- 与其他章关系：Ch.1/Ch.2 → Ch.7 量→特征→分类；Ch.3 → Ch.7 6D Pose 在合格集判定；Ch.8 OCR/Ch.9 Data Code 是 Ch.7 的多分类特化

## 第 8 章 Control 算子体系总结（提交 279ee8d）

- 用户要求：发截图（第 8 章 Control），继续用 mindmap-generator-plus skill 出图
- 算子拆解（按截图严格对位 35 算子）：
  * 族 A · Assignment 赋值与变量（4）: assign / assign_at / insert / global
  * 族 B · Conditional 条件分支（9）: if / else / elseif / endif / ifelse / switch / case / default / endswitch
  * 族 C · Loops 循环（8）: for / endfor / while / endwhile / repeat / until / break / continue
  * 族 D · Exception 异常处理（4）: try / catch / throw / endtry
  * 族 E · Procedure 过程与并行（4）: import / export_def / return / par_join
  * 族 F · Tuple<->Vector 元组↔向量（2）: convert_tuple_to_vector_1d / convert_vector_to_tuple
  * 族 G · Flow Misc 流程杂项（4）: executable_expression / stop / exit / comment
  * 合计 35 算子
- 关键差异（与 Ch.1-Ch.7 完全不同）：
  * **本章不是图像算子**，而是 HDevelop 脚本语言级关键字（与 C/Python 的 if/for/while/try 同类）
  * 导出 C++/Python/.NET 后**全部被翻译成目标语言原生控制流**，运行时不存在
  * ifelse 是 HDevelop 独有的**三元表达式**（类似 C 的 ?:），不是关键字
- 产物：
  - 07-算法/HALCON/章节总结/08-控制.md（510 行，21.4 KB）
  - 07-算法/HALCON/章节总结/08-控制.png（315 KB，**七边形辐射布局** mindmap，中心 Control，7 族分布在 7 个方向，每族一个子组 box 内列算子；英文子组标题如 "Assign & Vars" / "If / Switch" / "For / While / Repeat" / "Try / Catch" / "Proc / Parallel" / "Tuple<->Vector" / "Stop / Exit / Comment"）
  - 索引更新：07-算法/README.md 加第 8 章行；根 README 第 8 章链接 +07 计数 7→8
- 思维导图布局迭代（v1 → v4）：
  * v1 七边形 + 算子切向铺开 → 算子与家族框重叠（家族框边沿与第一个算子贴边）
  * v2 减小算子宽、加大间距 → 仍有重叠
  * v3 动态算子宽度（按名字长度）→ Conditional 9 个算子 tangent spread 太大
  * v4 **改用『家族框 + 子组 box + 算子纵向列』** → 各族独立子组 box 沿径向外，box 内部纵向列算子；header 是英文族描述
- 关键认知（文档重点强调）：
  * `switch` **不会**自动 break，必须显式 `break`，否则穿透
  * `for I := 0 to 9 by 1` 是半开区间 [0,9) 共 9 次（End 不包含）
  * `return` 终止整个过程，不是当前 if 块
  * `par_join` 只等待 `par_start` 启动的线程，单用会卡死
  * `global` 声明必须在过程外或主程序入口
  * `ifelse` 只能单行赋值，不能独立成块
  * `export_def` 只在**导出代码时**插入文本，运行时无作用
- 文档结构（10 节）：这一章在讲什么 / 7 族总览 / 各族详解 / 签名全集 / 与其他章的关系 / 速查卡 / 8 条常见误区 / 跨语言导出对照（C++/Python/.NET）/ 备注
- 推送：279ee8d（从 9f1a84e → 279ee8d，+516/-1，4 文件：510 行 md + 315KB PNG），SSH 推送成功，本地与远端 SHA 完全一致 ✅
- WebFetch 验证 raw.githubusercontent.com 可访问第八章内容（中文路径 URL 编码）

## 第 10 章 Develop 完成（用户发"第10章"截图标题为 Develop）
- 算子数：42 = 手册 toc 36 + 6 个 dev_map_*/dev_unmap_*（HDevEngine 内部 API，toc 缺失但 .html 存在）
- 8 族分组（与 Ch8 Control 同属"非图像算子"）:
  1. Window Mgmt (6): dev_clear/close/get/set_window(_extents) + dev_open_window
  2. Display Drawing (10): dev_display + dev_set_color/colored/draw/line_width/shape/paint/contour_style/lut/part
  3. Text & Dialog (3): dev_disp_text (唯一有 GenParam 的) + dev_open_dialog + dev_open_file_dialog
  4. Var & Memory (8): dev_clear_obj + dev_update_var + 6 个 dev_map/unmap_*（HDevEngine）
  5. Error & Except (3): dev_error_var + dev_get_exception_data + dev_set_check
  6. Update Control (3): dev_update_pc/time/window（演示模式提速三件套）
  7. Tools & Inspect (6): dev_open/close/set_tool_geometry + dev_show_tool + dev_inspect/close_inspect_ctrl
  8. System & Pref (3): dev_get/set_preferences + dev_get_system
- **关键事实**（文档头部强调）：所有 dev_* 算子**只在 HDevelop IDE 或 HDevEngine 内部有效**，导出 C++/Python/.NET 后：
  * 19 个 ✅ 可移植（dev_display/dev_set_color/dev_open_window 等绘图/窗口类）
  * 17 个 ❌ 失效（dev_inspect_ctrl/dev_update_* / dev_*_tool 等 IDE UI 类）
  * 6 个 ⚠️ 仅 HDevEngine（dev_map_*/dev_unmap_*）
- 判定依据：HTML `sec_synopsis` 段是否同时含 C/C++ 库函数签名 vs 只有 HDevelop 段
- 思维导图：8 边形辐射式（8 角各 1 卡片，**卡片高度按算子数自适应**），18 ops 最多的 Display Drawing 卡片最高
- 8 条误区重点：导出代码里调 dev_* 报"Operator not found" / dev_set_* 后没 dev_display 不显示 / dev_update_window('off') 忘 on / 中文乱码需 GenParamName='font' / dev_inspect_ctrl 不暂停程序
- 文档结构（9 节，简化版）：本章定位 / 8 族总览 / 各族详解 / 完整签名速查（42 算子 + 可移植性列）/ 与其他章关系 / 速查卡 / 8 条常见误区 / 跨语言导出对照 / 备注
- 推送：fdf5a0b（从 279ee8d → fdf5a0b，+378/-1，4 文件：377 行 md + 511KB PNG），SSH 推送成功，本地与远端 SHA 完全一致 ✅
- WebFetch 验证 raw.githubusercontent.com 可访问第十章内容（中文路径 URL 编码）
- 与前 9 章关系：Ch1-7 是图像算子；Ch8 Control 是脚本语言关键字（导出翻译为目标语言原生）；Ch10 Develop 是 IDE 工具 API（多数导出后失效）—— 三类截然不同的"算子"

## 第 11 章 File I/O 总结（Ch11，commit a7d332b，2026-08-21 17:50）
- 截图：**7 子族 51 个算子**（Access/Images/Misc/Object/Region/Tuple/XLD），toc 文件分 7 个 toc_file_*.html
- **算子入族**：完全按 toc_*_file.html 抽取（toc 抓 HDevelop name），共 51 个；其中 Access 实际 11 个（含 fread_serialized_item/fwrite_serialized_item 两个 SerializedItem 流算子，截图未列），其余 6 子族算子数与截图一致
- 本章核心特征：**导出后 51 个算子全部可移植**——不像 Ch8（翻译成目标语言控制流）或 Ch10（dev_* 多失效），File 系列算子本质是 HALCON 引擎 IO，导出后仅命名风格变（C++ 驼峰 / Python 下划线）
- **签名抽取模板**：用 `id="sec_synopsis"` 段而非 `<h2>Signature`（后者 HDevelop 章节叫 Control 没有），同时抽 5 语言 binding（HDevelop/C/C++/Python/.NET）
- **参数抽取模板**：必须用 `id="sec_parameters"` 锚点的正则，且 `class="parname"` 之前是 `id="ParamName"`（不是 `class` 紧跟）；遇到 `GenParamName/GenParamValue` 三件套的算子（read_*xld_dxf）单独抓 List of values + Suggested values 段
- **文档结构（10 节）**：本章定位（含与 Ch8/Ch10 的导出可移植性对比）/ 7 族速览表 / 各族详解（含 14 参 read_sequence 的参数对照表 + DXF 算子的 DxfStatus 状态码）/ 完整签名速查 51 行 / 与 Ch15/17/19/21/27 的关系 / 速查卡（3 张：1 行/3 路径/全局副作用）/ **8 条误区**（write_image 靠 Format 不是扩展名 / fread_string 不读整行 / fwrite_bytes 接受 0-255 字节或 string / read_world_file 靠 .tfw 同名 / read_sequence Index 是帧号 / close_file 不自动 / tuple_is_serializable 之前要先 serialize_ 子对象 / read_image 不读目录）/ 跨语言导出对照表 / 备注
- **本节额外重点**：set_current_dir 调的是 OS 进程 cwd 不是 HALCON 局部；list_files 是批处理遍历的核心且 Options 多值可自由组合；DXF/ARC 是 CAD/GIS 双向桥梁；fread_serialized_item + fwrite_serialized_item 做流式 SerializedItem 持久化（与 serialize_* 配对）
- 推送：a7d332b（从 fdf5a0b → a7d332b，+540/-1，4 文件：378 行 md + 85KB PNG），SSH 推送成功，本地与远端 SHA 完全一致 ✅
- WebFetch 验证 raw.githubusercontent.com 可访问第十一章内容（中文路径 URL 编码）

## 第 12 章 Filters 拆为上/中/下 三卷（Ch12，commit 94053a3，2026-08-21）
- 用户截图：第 12 章 Filters 是全书最厚章节，要求「分成三个文档」「思维导图更加清晰」「要有中文」「完整签名速查要有简洁的算子功能介绍」
- 切片依据（按算子性质 + 数量均衡）：
  * **上卷 41**：像素级独立计算 —— Arithmetic 算术代数 / Bitwise 位运算 / Color 颜色空间（三角函数 / Mult-Add / LUT / PCA 全部要点）
  * **中卷 48**：邻域 + 空间重采样 —— Edges 边缘 / Enhancement 增强 / Inpainting 修补 / Geometric Transforms 几何变换（Canny/Prewitt/Sobel/LoG/DoG + 各种扩散 + 各种修补 + 各种投影）
  * **下卷 98**：频域 + 空域 + 特征 —— FFT 频域 / Smoothing 空域平滑 / Wiener / Noise·Misc / Points(Harris/Foerstner) / Lines(Hough) / Match / OpticalFlow / SceneFlow / Texture
  * 合计 187 算子（18 子族），通过 toc_filters_*.html 本地目录枚举核对
- 中文思维导图：沿用 matplotlib + SimHei 字体注入（拷贝 simhei.ttf 到 venv 字体目录 + 清 ~/.matplotlib 缓存），双语标签（中心中文 + 算子英文名）
  * 上：三角辐射（3 族）
  * 中：四角放射（4 族，legend 移右侧防重叠）
  * 下：3 大超族（Frequency / Spatial / Features+Flow）辐射，每超族卡片列「族名 + 算子数 + 3 示例算子」避免溢出
- 签名速查创新：按用户要求改为「算子 | 一句话功能 | HDevelop 关键签名」表格（不再是 5 语言长签名全文），满足「简洁的算子功能介绍」
- 文档结构（每卷 9–10 节）：定位 / 族总览 / 各族详解 / 速查卡 / 常见误区 / 跨语言对照 / 完整签名速查（简洁功能列）/ 备注；三卷互引
- 产物：12-滤波(上/中/下).md（362/408/573 行）+ 三个 PNG 思维导图；07-算法/README.md 与根 README 索引均新增 3 行
- 推送：94053a3（从 a7d332b → 94053a3，branch main = origin/main，remote HEAD SHA `94053a3` 一致），SSH 推送成功 ✅