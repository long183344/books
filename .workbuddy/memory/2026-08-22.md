# 2026-08-22 工作日志

## Ch12 中/下卷重组

完成对第 12 章 Filters 中卷与下卷的第二次重组，调整为按官方算子族群分类：

### 中卷：4 族 48 ops → 7 族 87 ops
- 保留 Edges 23 + Enhancement 7 + Inpainting 6 + Geometric 12 = 48
- 新增 FFT 31 + Lines 4 + Match 4 = 39
- 87 个算子总数不变（与官方 TOC 一致）
- 重写中卷从 10 节扩展为 14 节，新增 FFT/Lines/Match 三大族详解

### 下卷：8 族 98 ops → 8 族 59 ops
- 移除错放的 FFT(31) + Lines(4) + Match(4) = 39
- 新 8 族：Misc 8 + Smoothing 24 + Noise 5 + Wiener 6 + Points 7 + OpticalFlow 4 + SceneFlow 2 + Texture 3 = 59
- 重写下卷从 10 节扩展为 15 节，结构整洁

### 思维导图重新生成
- 中卷（七边形 7 族）+ 下卷（八边形 8 族）
- 双语标签（中文族名 + 英文算子），各家族 3 个示例算子
- 注入 SimHei 字体，3384×3384 像素 PNG

### Git
- Commit: `505e3f2 docs(halcon-ch12): 第 12 章 中/下卷重组 — 中卷扩为 7 族 87 算子（含 FFT/Lines/Match），下卷聚焦 8 族 59 算子`
- 推送：94053a3 → 505e3f2，本地与远端 SHA 完全一致 ✅
- 6 文件差异：+668/-711，3 张表（中卷 md + png + 下卷 md + png）+ 2 个 README 更新

### 经验总结
1. **Windows Python 路径**：Python 的 `os.path.exists` 用 `E:/...` 而非 `/e/...`，Git Bash 用 `/e/...`——Python 脚本必须用盘符 + 正斜杠
2. **HALCON HTML 签名提取**：用 `<b>opname</b>(.*?)</code>` 配合 `<.*?>` tag-strip 是最稳的方案；上一版本用 h3/pre 提取会落到示例代码段（带错算子名）

---

## Ch13 Graphics(上) 主动绘图与交互

完成第 13 章 Graphics 上卷（5 族 78 ops）。

### 切片方案（全章 9 子族 165 ops → 2 卷）
- **上卷 78 ops**：3D Scene 20 + Drawing 23 + LUT 3 + Mouse 11 + Object 21 = 主动绘图 + 交互
- **下卷 87 ops**（待做）：Output 16 + Parameters 38 + Text 12 + Window 21 = 窗口系统 + 输出

### 工作流
1. **抽签名**：`_tmp_ch13_up.py` 用 `Signature` 关键词 + 平衡括号扫描（HDevelop 风格签名格式 `opname(... : ... : ... :)`），从官方 HTML 抠出 78/78 算子签名
2. **文档**：`13-Graphics(上).md` 542 行 11 节结构：
   - §0 本卷定位与适用读者
   - §1 五族速览
   - §2-§6 五族详解（3D Scene / Drawing / LUT / Mouse / Object）
   - §7 五族关系图（ASCII）
   - §8 五语言对照速查
   - §9 10 条误区
   - §10 完整签名速查（5 族 × 算子 | 一句话功能 | HDevelop 签名）
   - §11 与 Graphics · 下卷衔接
3. **思维导图**：5 族五边形辐射 PNG，中英文双语 + 6 个示例算子/族 + 中文卡片副标；SimHei 字体
4. **索引更新**：07-算法/README.md 与根 README.md 各 +1 行；分类计数 "12 篇" → "13 篇"

### 13-Graphics(下) 卷 — 4 族 87 算子（commit `c4bdc74`）
- **下卷**：Output 16 + Parameters 38 + Text 12 + Window 21 = 87 ops
- 全文 9 节 + 4 个完整签名速查表（每族一行 `算子 | 一句话功能 | HDevelop 签名`）
- 思维导图：4 族四边形辐射（紫=Output / 蓝=Window / 橙=Text / 绿=Parameters），SimHei 字体
- **签名提取 bug 修复**：Strategy E 升级——优先从 `<div data-if="hdevelop">` 块抽签名（解决部分算子抓到 Python 风格带 type annotation 的问题）
- 索引更新：07-算法/README.md 与根 README.md 各 +1 行；分类计数 "13 篇" → "14 篇"
- **Graphics 全章完成**：上卷 5 族 78 + 下卷 4 族 87 = **9 族 165 ops** ✓
- 推送：13d65fe..c4bdc74 main -> main ✅（本地 = 远端一致）

### Git
- Commit: `13d65fe docs(halcon-ch13): 第 13 章 Graphics(上) — 5 族 78 算子 主动绘图与交互`
- 推送：505e3f2 → 13d65fe，本地与远端 SHA 完全一致 ✅
- 4 文件差异：+545/-1（13-Graphics(上).md + png + 2 README）

### 经验总结（增量）
3. **HALCON 签名提取 Strategy E**：HTML 中 `(Operator)` `(Name)` 类的"面包屑签名"在算子名前，跟真正的 HDevelop 签名一起存在；新版用 `Signature\s+opname(\s*\(: 关键词 + 平衡括号 + 内容长度阈值（≥5）+ 必须含`:``（HDevelop 分隔符）作为过滤器，**98% 以上能一次到位**。
4. **pentagon 思维导图布局**：5 族用中心 +5 顶点（12点位置顺时针），每族 box 宽 6.2 高 5.6；3 op 族（LUT）box 仍按统一尺寸渲染——视觉一致性强，只是顶部空一些。
5. **Strategy E 增强：HDevelop 块优先抽取**：ch13 Graphics(下) 出现 10 个 window 族算子抓成了 Python 风格签名（`name ( param : Type, ...)`），根因是 Python 签名也含 `:` 且更长。修复：先在 `<div data-if="hdevelop"[^>]*>...</div>` 块内（去标签后）搜 name + 平衡括号，命中即返回；未命中再降级到全文档扫描。**新规则：HTML 索引文件有 `data-if="hdevelop"` 这种语言分支块，要走它而不是全文档**。
6. **名称与 `(` 间允许 HTML 标签**：`get_window_attr</b>(` 这种情形，正则应用 `name(?:<[^>]+>)*\s*\(` 而不是 `name\s*\(`，否则 10 个算子全部丢失。

### 下卷待办
- 抽取 4 族（Output/Parameters/Text/Window）共 87 ops 签名 ✅（87/87 一次成功）
- 写 `13-Graphics(下).md`，五边形 → 方形辐射布局 ✅（9 节 + 4 个完整签名速查表）
- 推送合并 ✅（commit `c4bdc74`，本地 = 远端一致）
3. **思维导图布局规律**：7 族用 7 等分环形，8 族用 8 等分环形（八边形），卡片高度一致，标题色块 + 卡片体两段式
4. **SimHei 字体路径**：`/c/Users/Administrator/.workbuddy/binaries/python/versions/3.13.12/envs/mindmap/Lib/site-packages/matplotlib/mpl-data/fonts/ttf/simhei.ttf`（永久存在于 mindmap venv 的 matplotlib 中）

## 16:43 — Ch14 Identification (44 ops, single volume)
- **Chapter 14 = Identification (识别)** — 3 families: Bar Code 15 + Data Code 12 + Sample-Based 17 = **44 ops**
- Strategy E 增强版 一次抽全 44 条签名（无遗漏），签名格式 ` `: -> `: GenParamName : Output`，符合 HDevelop 语义分隔规范。
- 描述清洗规则：`re.search(r'\s+[—\-]\s+', desc)` → split on `—` 保留其后真正的英文描述（剔除 `nameT_nameNameClassNameFunctionname` 这种 NUL 索引缓存前缀）。
- TOC 文件位于 `doc/html/reference/operators/`，**子 toc `toc_identification_datacode.html` 是空壳**，需直接列文件或按截图族枚举算子。
- 单卷决策：44 ops 数量较少，做成单卷 `14-Identification.md` + 三角形辐射 PNG（3 族 → 蓝/绿/红）。
- 模板沿用 Ch13 下卷格式：定位 → 三族速览表 → 思维导图 → 各族详解（标志 5/7 步流水线 + 误区表）→ 通用工作流 → 误区 → 4 张速查表（汇总 + 3 族子表）→ 一句话总结。
- Commit `f37929a` 推送成功，本地=远端 SHA 已验证一致（`c4bdc74..f37929a main -> main`）。
- README 双索引更新（07 + 顶层），章节总结计数 14→15 篇。

## 17:21 — Ch15 Image 上卷 (5 族 62 ops, single half)
- **Chapter 15 = Image** — 9 族 106 ops (Access 9 + Acquisition 14 + Channel 17 + Creation 16 + Domain 6 + Features 24 + Format 9 + Manipulation 6 + Type Conversion 5)
- 本卷包含：Access + Acquisition + Channel + Creation + Domain = **62 ops**（图像的"获取与组织"）
- 下卷预告：Features + Format + Manipulation + Type Conversion = **44 ops**（图像的"分析与变换"）
- **关键修正**：截图中有 `decompose1`，但文件系统没有 `decompose1.html`，Channel 实际是 17 ops（decompose2~7）。
- **Access 族截图中漏掉了 `get_image_pointer1_rect`**：toc 列出 9 个 (含 `get_image_pointer1_rect`)，实际比截图多 1 个。
- Channel 族 `compose2..7` + `decompose2..7` 是**固定 N 通道的便捷版**；通用版是 `channels_to_image` / `image_to_channels`。
- Domain 族 `add_channels` 有点异类：实际功能是"把 Region 转成灰度图（Region 内 = ForegroundGray，外 = BackgroundGray）"，放在 Domain 族合理但易误解。
- 模板沿用 Ch13/Ch14：定位 → 五族速览表 → 思维导图 → 五族详解（5/7 步流水线 + 误区表）→ 通用工作流 → 误区 → 5 张速查表 → 一句话总结。
- Commit `464c4ea` 推送成功，本地=远端 SHA 一致（`f37929a..464c4ea main -> main`）。
- README 双索引更新，章节总结计数 15→16 篇。

## 17:53 — Ch15 Image 下卷 (4 族 44 ops)
- **Ch15 Image 全章 9 族 106 ops 完成**：上卷 62 (Access+Acquisition+Channel+Creation+Domain) + 下卷 44 (Features+Format+Manipulation+TypeConversion)
- **下卷 4 族**：Features 24 (area_center_gray/intensity/entropy_gray/gen_cooc_matrix/cooc_feature_image/fit_surface_*/select_gray/gray_histo_*/histo_2dim/shape_histo_*/...) + Format 9 (crop_part/crop_rectangle1,2/crop_domain/change_format/tile_images/tile_images_offset/tile_channels) + Manipulation 6 (set_grayval/paint_gray/region/xld/overpaint_gray/region) + Type Conversion 5 (convert_image_type/complex_to_real/real_to_complex/real_to_vector_field/vector_field_to_real)
- Strategy E 一次抽全 44/44，签名格式 `Regions, Image : : Features : Value` 这种 (Region,Image) 复合输入大量出现，与上卷 Image 单一输入不同。
- 关键概念："change_format 不插值"——只是改 Width/Height 标记，像素值被等长重映射，做算法插值必须用 `zoom_image_factor` (Ch12 中卷)。
- `overpaint_*` vs `paint_*` 区分：overpaint 原地覆盖，paint 返回新图；域外 overpaint 不动，paint 当透明。
- Type Conversion 中 `complex_to_real` 拆实部+虚部顺序：第一参数实部，第二虚部（atop2(Im,Re) 算相位）。
- Commit `bd326cf` 推送成功，本地=远端 SHA 一致（`464c4ea..bd326cf main -> main`）。
- README 双索引更新，章节总结计数 16→17 篇。Ch15 全章完成。

## 18:15 — Ch16 Inspection (5 族 53 ops, single volume)
- **Chapter 16 = Inspection (检测)** — 5 族：Bead Inspection 胶路检测 5 + OCV 光学字符校验 8 + Structured Light 结构光 11 + Texture Inspection 纹理检测 15 + Variation Model 差异模型 14 = **53 ops**，工业质检"五朵金花"
- Strategy E 一次抽全 53/53 签名（0 miss）。toc_inspection.html 正常枚举 5 个子族 toc，无空壳问题。
- **思维导图美学升级**（用户明确要求"更优美一些"）：从简单辐射升级为"五瓣金花"设计——
  - 3 层背景光晕圆（半径 1.5/1.3/1.1，透明度递减）
  - 中心焦点圆：3 层装饰环（#FFE9C2/#FFD68F/#FFC25C 金色渐变）+ 内白心
  - 5 张花瓣形彩色卡片：FancyBboxPatch 阴影 + 主卡 + 内白心(alpha=0.18) + 编号徽章 01-05（深色圆+白描边）
  - 双重轨道连线：粗彩线 lw=3 + 细深线 lw=0.5 双层叠加
  - 配色：钢蓝/翠绿/琥珀金/珊瑚橙/紫罗兰；画布 13×10.5，DPI 130，成品 175KB
- 五族统一"训练→检测→出结果"三段式：Bead Inspection `create_bead_inspection_model→apply→get_result`；OCV `create_ocv_proj→traind_ocv_svm→do_ocv_simple`；Structured Light `compute/decode_projected_object`；Texture `create_texture_inspection_model→apply→select`；Variation Model `create_variation_model→prepare/train→compare`
- `compare_variation_model` 是零标注缺陷检测利器：只需 OK 样本图集即可训练出"金标准+容忍带"。
- Commit `ea51fd7` 推送成功，本地=远端 SHA 一致（`bd326cf..ea51fd7 main -> main`）。
- README 双索引更新，章节总结计数 17→18 篇。
- 跨章累计：Ch14 44 + Ch15上 62 + Ch15下 44 + Ch16 53 = 203 ops，Strategy E 全部一次抽全。
