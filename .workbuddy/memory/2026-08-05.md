# 2026-08-05 工作日志

## 个人笔记库 → GitHub 推送（进行中）
- 本地仓库：E:\ProgramData\WorkBreak\2026-08-04-09-51-45，已 git init（main 分支）
- 已完成：23 个文件、2830 行的笔记库（7 大分类 + 计算机硬件 13 篇），本地提交 `724e5c1`
- 目标远程：github.com/long183344/books（空仓库，用户已建），remote 已切到 SSH：`git@github.com:long183344/books.git`
- 阻碍与结论：
  - GitHub 连接器为**只读**（create_repository / push_files 均 403），不可用于写。
  - 环境无 GITHUB_TOKEN / 凭证缓存，HTTPS git push 无法认证。
  - 唯一可行路径 = SSH。已在本机生成 ed25519 密钥对：私钥 `C:\Users\Administrator\.ssh\ai`（ACL 已收紧为仅 Administrator），公钥已交给用户去 GitHub → Settings → SSH and GPG keys 添加。
- 完成：用户将公钥加入 GitHub 后，`git push -u origin main` 成功，远程 `main` = `724e5c1`（与本地一致，23 文件原样上库）。提交信息 `feat: 搭建个人笔记库并完成计算机硬件专题（13 篇）`。
- 注意：Windows 上 chmod 对 NTFS 无效，需用 icacls 收紧私钥 ACL，否则 SSH 报 UNPROTECTED PRIVATE KEY FILE。
- 验证要点：推送后 `git push` 曾显示 "Everything up-to-date" 误导，但 `git ls-remote` 证明远程 main 已指向 724e5c1；用 `git ls-remote origin refs/heads/main` 取远程 SHA 与本地比对，相同即确认上库成功（git 内容寻址，SHA 同=文件树同）。
